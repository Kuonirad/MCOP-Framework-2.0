# Master Audit (2026-05-01) — Remediation Tracker

**Companion to:** [`master-audit-2026-05-01.md`](master-audit-2026-05-01.md)
**Reconciliation date:** 2026-05-01
**Reviewer:** maintainer pass against the repository's actual state on the
audit-receipt date (commit `84c8c03` on `main`).

This tracker reconciles each Priority 1–4 item from the master audit
report against the repository's **actual** state. Several items the
auditor flagged as gaps were already closed before the audit was
issued — those are marked **already-landed** with a pointer to the
file or commit that satisfies them.

---

## Priority 1 — within 14 days

| # | Audit recommendation | Reality on 2026-05-01 | Status |
|---|----------------------|------------------------|--------|
| 1 | Add `.github/dependabot.yml` and enable automated PRs | Present at [`.github/dependabot.yml`](../../.github/dependabot.yml) — weekly updates for `npm`, `github-actions`, `docker`, with grouped dev / prod / actions updates | ✅ already-landed |
| 2 | Publish coverage badge and enforce ≥95% in CI | Badge committed at [`docs/badges/coverage.svg`](../badges/coverage.svg) and rendered in [`README.md`](../../README.md) line 5; generated by [`scripts/coverage-badge.mjs`](../../scripts/coverage-badge.mjs); coverage uploaded to Codecov from [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) | ✅ already-landed |
| 3 | Attach Sigstore provenance to all future releases | npm publish workflow uses **Trusted Publishing + automatic provenance** via Sigstore — see [`.github/workflows/publish-npm.yml`](../../.github/workflows/publish-npm.yml) (`id-token: write`, `--provenance`); PyPI workflow at [`.github/workflows/publish-pypi.yml`](../../.github/workflows/publish-pypi.yml) | ✅ already-landed |

## Priority 2 — within 30 days

| # | Audit recommendation | Reality on 2026-05-01 | Status |
|---|----------------------|------------------------|--------|
| 4 | Deploy TypeDoc to a hosted docs site | TypeDoc generation wired via `pnpm typedoc` (root `package.json`); generated output checked in under [`docs/api/`](../api/); a hosted GitHub Pages deployment is **not yet wired** | 🟡 partial |
| 5 | Add Lighthouse CI + performance budgets | **Net-new in this PR** — [`.github/workflows/lighthouse.yml`](../../.github/workflows/lighthouse.yml) and [`lighthouserc.json`](../../lighthouserc.json) added; runs on PRs and main; assertions cover performance, a11y, best-practices, SEO | ✅ landed (this PR) |
| 6 | Create and link full `GOVERNANCE.md` and `ROADMAP.md` | [`GOVERNANCE.md`](../../GOVERNANCE.md) exists and is linked from [`README.md`](../../README.md) line 12 (Maintained badge) and line 181 (Contributing section); roadmap preserved as [`ROADMAP_TO_100.md`](../../ROADMAP_TO_100.md) (marked historical, points readers at `GOVERNANCE.md` + `CHANGELOG.md` for live priorities) | ✅ already-landed |

## Priority 3 — within 60 days

| # | Audit recommendation | Reality on 2026-05-01 | Status |
|---|----------------------|------------------------|--------|
| 7 | Integrate mutation / fuzz testing and OpenTelemetry | Not yet wired. Tracked for next quarter — requires ADR for OTel collector topology and a decision between Stryker (mutation) and fast-check (property-based) for the core encoders | ⏳ planned |
| 8 | Add i18n scaffolding and a11y CI gate | a11y gate now covered by Lighthouse CI added in this PR (assertion `categories:accessibility ≥ 0.95`). i18n scaffolding deferred — no concrete locale demand yet; revisit after first non-English contributor lands | 🟡 partial |
| 9 | Seed "good first issues" and activate the all-contributors bot | Per [`docs/audits/governance-review-2026-Q2.md`](governance-review-2026-Q2.md) §Issue / PR backlog: 10 open issues, all `good-first-issue`. `.all-contributorsrc` exists at repo root | ✅ already-landed |

## Priority 4 — ongoing

| # | Audit recommendation | Reality on 2026-05-01 | Status |
|---|----------------------|------------------------|--------|
| 10 | Publish quantitative benchmark results and case studies | [`docs/benchmarks/results.json`](../benchmarks/results.json), [`docs/benchmarks/methodology.md`](../benchmarks/methodology.md), and [`docs/CASE_STUDIES.md`](../CASE_STUDIES.md) all present; CI snapshot drift guard documented in the 2026-04-30 audit response | ✅ already-landed |
| 11 | Explore formal verification (TLA+ / Lean) for core invariants | Initial work under [`docs/formalization/`](../formalization/) | 🟡 partial |

---

## Audit Errata (factual corrections)

The master audit report contained a small number of factual inaccuracies
relative to the repository's actual state on the receipt date. They are
recorded here for completeness:

1. **"Dependabot.yml not explicitly confirmed in tree"** — file is present
   at [`.github/dependabot.yml`](../../.github/dependabot.yml) and has been
   for multiple releases.
2. **"`GOVERNANCE.md` not prominently linked in top-level README"** — it is
   linked twice in [`README.md`](../../README.md) (badge on line 12, prose
   link on line 181).
3. **"No public code-coverage badge"** — present and rendered in the README
   header (line 5), backed by Codecov upload from CI.
4. **"No matrix testing across Node/Python versions visible"** — the main
   workflow [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml)
   runs on Node 20.x + 22.x and Python 3.10 + 3.12.
5. **"Full Sigstore attestation not attached to every release note"** —
   npm trusted publishing automatically generates Sigstore provenance for
   every release; PyPI uses OIDC trusted publishing.

These errata do not change the auditor's overall headline score; they
narrow the genuine remaining-work surface to mutation / fuzz testing,
OpenTelemetry, i18n, hosted TypeDoc deployment, and (now closed in this
PR) Lighthouse CI.

## Second Audit (received 2026-05-01) — findings reconciliation

A second audit landed on the same day with a "Critical Findings Summary"
table. Each row was verified against the repository:

| Severity (per auditor) | Claim | Verification | Action taken |
|------------------------|-------|--------------|--------------|
| 🔴 CRITICAL | `uuid` overridden to `^14.0.0` (non-existent version) | uuid 14.0.0 was published; verified via `npm view uuid versions`. Auditor's knowledge cutoff predates the release | None — claim incorrect |
| 🔴 CRITICAL | `src/core/testing-utils.ts` is test-only and ships in production | File header documents it as a runtime SSR / LCP HTML inspector consumed by `scripts/verify-ssr-lcp.mjs` (CI) and jest specs alike; the "testing" in the name describes purpose, not scope | None — claim incorrect |
| 🔴 CRITICAL | Dual lockfiles (`package-lock.json` + `pnpm-lock.yaml`) | **Real regression.** PR #534 explicitly removed `package-lock.json` ("pnpm project"); PR #537 (Magnific migration) re-added it accidentally. CI workflows use `npm install --no-package-lock` — they actively avoid it | ✅ Removed `package-lock.json` in this PR |
| 🟠 HIGH | No ML runtime dependencies despite ML-named modules | By design — `NovaNeoEncoder` is deterministic hashing (not inference), `StigmergyV5` is cosine vector math, `tensorGuard` validates shapes only. Optional `openai>=1.0` extra exists in `mcop_package/pyproject.toml` for downstream LLM consumers | None — claim incorrect |
| 🟠 HIGH | `@kullailabs/mcop-core` namespace inconsistency vs `@kuonirad` | Intentional dual identity: the repo is personal (`@kuonirad/mcop-framework` — the Next.js app), the published library is org-scoped (`@kullailabs/mcop-core`). Documented in `GOVERNANCE.md` | None — claim incorrect |
| 🟠 HIGH | `stigmergyV5.ts` — version in filename is an anti-pattern | Intentional protocol-generation marker (cf. HTTP/2, IPv6). Documented in `PLAIN_ENGLISH_GLOSSARY.md` under "Stigmergy v5 / Resonance" | None — claim incorrect |
| 🟡 MEDIUM | BUSL-1.1 relicense from MIT — contributor IP risk | Already addressed: `LICENSE-MIT-LEGACY` preserves pre-2026-04-26 commits under MIT; `NOTICE.md` documents the transition; `CONTRIBUTING.md` requires DCO sign-off | None — already mitigated |
| 🟡 MEDIUM | Concurrent E2E in `test:hybrid` causes flakiness | `test:hybrid` is a developer-convenience script (run-locally), not the CI entry point. CI uses sequential `pnpm test`, `pnpm cypress:run`, etc. via separate jobs | None — non-issue in CI |
| 🟡 MEDIUM | Dual test directories (`src/__tests__/` and `tests/`) | `src/__tests__/` holds unit specs co-located with source; `tests/` holds integration specs; `cypress/` holds E2E. Standard three-tier convention | None — claim conflates conventions |
| 🟡 MEDIUM | 15+ Markdown docs at root with no freshness mechanism | `scripts/shared-docs-guard.mjs` (`pnpm docs:guard`) enforces freshness; `pnpm parity:check` validates cross-doc parity | None — already automated |

## Net Outstanding Work After This PR

1. Wire a GitHub Pages (or equivalent) deploy for the `docs/api/` TypeDoc
   output — a workflow only, no source changes required.
2. ADR + implementation for OpenTelemetry instrumentation of the triad.
3. Choose between Stryker (mutation) and fast-check (property-based) for
   the core encoder; land an initial run.
4. Decide whether to defer i18n until concrete non-English contributor
   demand materializes (current recommendation: defer).

---

*Tracker maintained by the maintainer-of-record. Update on each audit cycle.*
