# v2.3.1 — mapping_grok Production Profile + ARC-EVO Benchmark

## Highlights

- Promotes `mapping_grok` to the default production orchestrator profile, targeting xAI/Grok with Stigmergy history injection, rate-limit retries, and MCOP pipeline hook support.
- Bakes the NOVA-EVOLVE-TUNER into the default orchestrator config with a 25-task validation split for patch-release reproducibility.
- Adds `pnpm benchmark:arc-evo`, a public deterministic benchmark that prints 25 ARC-style validation tasks, genome evolution decisions, Merkle roots, and latency traces.
- Expands public discovery metadata with `arc-agi`, `meta-cognitive`, and `evolutionary-ai` topics/keywords.

## Validation

Run:

```bash
pnpm benchmark:arc-evo
pnpm test -- adapters config
pnpm typecheck
```
