# v2.4.0 - Substrate Readiness Release

**Release recommendation:** cut `v2.4.0` from `main` once CI is green and no
pre-release TODO markers remain in the headline subsystems.

## Headline Features

- Proteome substrate and LS20 ARC scaffold
- Drift Sentinel Kernel telemetry
- Grok organelle host
- Redis Streams gossip transport
- ImpactAuditor positive-recursion stack
- CUDA Phi5 auto-probe with committed reference ONNX kernels

## Readiness Gates

| Gate | Evidence | Status |
| --- | --- | --- |
| Pre-release marker scan | Search `docs`, `src`, `packages`, `scripts`, `.github`, and `models` for pre-release TODO comments | Must be empty before tag |
| Positive recursion | `pnpm positive:verify` and `pnpm positive:quality` | Merge-blocking CI gates |
| CUDA structural artifacts | `models/mcop_*.onnx`, `models/manifest.json`, `docs/cuda/kernels-manifest.json` | Reference artifacts committed |
| Real GPU promotion | `pnpm soak:cuda-verified-device` on a GPU host for 1,000 steps | Required before changing CUDA status from scaffolded to production-done |
| Reproducible benchmark trust | `docs/benchmarks/EXTERNAL_REPLAY.md` | Needs first verified external replay |

## CUDA Status

The committed `models/mcop_*.onnx` files are deterministic reference artifacts
from `scripts/export_cuda_kernels/export.py --backend reference`. They close the
inventory and manifest gap, but they are not trained production kernels. Keep
the CUDA layer described as scaffolded until a real GPU runner completes the
1,000-step verified-device soak with zero `GhostGPUError` events.

## Release Note Copy

MCOP v2.4.0 ships the expanded trust substrate: Proteome substrate dynamics,
Drift Sentinel divergence telemetry, organelle-hosted Grok execution, Redis
Streams transport, ImpactAuditor recursion, and CUDA Phi5 auto-probe support
with committed reference ONNX artifacts and Merkle-rooted kernel manifests.
