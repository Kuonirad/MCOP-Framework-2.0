# Preprint figures

Figures emitted by [`examples/reproducible-benchmark/notebooks/reproduce-22700-ops.ipynb`](../../../../examples/reproducible-benchmark/notebooks/reproduce-22700-ops.ipynb).

The notebook writes them into `examples/reproducible-benchmark/out/figures/` —
copy the latest set into this directory at submission time so the rendered
preprint references the same artefacts the manifest covers.

Expected files:

* `figure-1-latency.png` — average end-to-end latency per prompting mode.
* `figure-2-triad-vs-llm.png` — `mcop-mediated` triad vs. LLM-call breakdown.
