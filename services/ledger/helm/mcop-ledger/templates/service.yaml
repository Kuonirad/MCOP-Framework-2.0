apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcop-ledger.fullname" . }}
  labels:
    {{- include "mcop-ledger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcop-ledger.selectorLabels" . | nindent 4 }}
