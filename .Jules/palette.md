## 2025-02-04 - Tailwind v4 Transform Issue
**Learning:** Tailwind CSS v4 in this specific Next.js/Turbopack configuration fails to apply `hover` transform utilities (translate, scale) despite valid syntax. `box-shadow` and `opacity` work correctly.
**Action:** Use `box-shadow` or `opacity` for interaction feedback instead of transforms until the configuration is debugged.
