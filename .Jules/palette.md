## 2026-02-22 - Consistent Focus State for Directional Links
**Learning:** Directional links with hover transitions (like `→` translating) must mirror that behavior on focus using `group-focus-visible`.
**Action:** Always pair `group-hover:translate-x-*` with `group-focus-visible:translate-x-*` on arrow icons inside links.
